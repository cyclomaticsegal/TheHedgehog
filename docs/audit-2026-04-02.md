<style>
body { max-width: 100% !important; width: 100% !important; padding: 0 2em; box-sizing: border-box; }
table { width: 100%; table-layout: fixed; border-collapse: collapse; }
th, td { word-wrap: break-word; overflow-wrap: break-word; padding: 6px 10px; vertical-align: top; border: 1px solid #444; }
th:nth-child(1), td:nth-child(1) { width: 7%; }
th:nth-child(2), td:nth-child(2) { width: 7%; }
th:nth-child(3), td:nth-child(3) { width: 28%; }
th:nth-child(4), td:nth-child(4) { width: 14%; }
th:nth-child(5), td:nth-child(5) { width: 28%; }
th:nth-child(6), td:nth-child(6) { width: 16%; }
</style>

# Audit: Security, Memory & Performance
**Date:** 2026-04-02 · **Scope:** All `.rs` files, `Cargo.toml`, `.env`

`[ ]` open · `[x]` done · `[-]` won't fix

---

## Security

| ID | Severity | Issue | File | Action | Status |
|----|----------|-------|------|--------|--------|
| SEC-C1 | Critical | Live API keys in `.env` — rotate FRED, Alpha Vantage, Tiingo | `.env` | Rotate keys; `.env` is gitignored and was never committed | [-] not required — repo is local only |
| SEC-C2 | Critical | `expect()` panic on DB init — app crashes with no user message if DB is locked/corrupted | `app.rs:120` | Replace with graceful error screen | [x] Falls back to in-memory DB; shows full-screen error panel with path, cause, and Quit button |
| SEC-H1 | High | API keys stored plaintext in SQLite | `models.rs:219`, `storage.rs` | Remove keys from DB; load from env only | [x] `ApiKeys` struct loaded from `.env` at startup; written back to `.env` on Save; DB never sees keys |
| SEC-H2 | High | No timeout on HTTP client — background thread blocks forever if API hangs | `providers.rs:17` | `Client::builder().timeout(Duration::from_secs(30))` | [ ] |
| SEC-H3 | High | No redirect policy — keys forwarded if DNS/BGP path is hijacked | `providers.rs:17` | `.redirect(reqwest::redirect::Policy::none())` | [ ] |
| SEC-H4 | High | Alpha Vantage API key appears in error messages (passed as URL query param) | `providers.rs:56` | Sanitise errors to strip query params before showing in UI | [ ] |
| SEC-H5 | High | DB file created with default umask — world-readable on shared systems | `app.rs:115` | `fs::set_permissions(&db_path, Permissions::from_mode(0o600))` on Unix | [ ] |
| SEC-M1 | Medium | `unwrap_or_default()` silently swallows DB errors — user gets no feedback | `app.rs:121, 205` | Surface errors in the status bar | [ ] |
| SEC-M2 | Medium | API responses accept `NaN`/`Inf` floats — propagates silently into analysis and charts | `providers.rs` (all parse sites) | Reject non-finite values after parsing | [ ] |
| SEC-M3 | Medium | No recursion depth limit on serde deserialisation — crafted DB value can stack-overflow | `storage.rs:59` | Validate JSON depth before deserialising | [ ] |
| SEC-M4 | Medium | No validation on threshold config — `approaching >= extreme` or `lookback_days == 0` accepted | `models.rs:194` | Add `ThresholdConfig::validate()` and call before saving | [ ] |
| SEC-M5 | Medium | No panic recovery in background refresh thread — silent failure, `refresh_in_progress` may stick | `app.rs:262` | Wrap thread body in `std::panic::catch_unwind`; send `FetchFailed` on panic | [ ] |
| SEC-M6 | Medium | Hardcoded `1500ms` Alpha Vantage rate-limit delay — unnamed magic constant | `providers.rs:40` | Extract to named constant `ALPHA_VANTAGE_REQUEST_INTERVAL` | [ ] |
| SEC-L1 | Low | No refresh cooldown — daily API quota (25 req/day) can be exhausted by rapid clicking | `app.rs:230` | Track `last_refresh_time`; enforce minimum interval with visible countdown | [ ] |
| SEC-L2 | Low | `danger_accept_invalid_certs` not explicitly set to `false` — TLS intent is implicit | `providers.rs:17` | Add `.danger_accept_invalid_certs(false)` as explicit documented no-op | [ ] |

---

## Performance & Memory

| ID | Severity | Issue | File | Action | Status |
|----|----------|-------|------|--------|--------|
| PERF-C1 | Critical | Summary string `format!()` allocated every frame from data that only changes on refresh | `app.rs:1207` | Cache in `DashboardApp`; recompute only when `cached_vix_status` changes | [ ] |
| PERF-C2 | Critical | Date range labels `format!()` called every frame in all 3 charts | `app.rs:1217, 1304, 2160` | Cache date labels; invalidate on window/zoom change | [ ] |
| PERF-C3 | Critical | `filter_for_zoom()` clones every `Observation` (inc. heap `String` field) every frame — ~390 clones/frame | `app.rs:1937` | Return `&[Observation]` slices or cache filtered result | [ ] |
| PERF-C4 | Critical | Correlation chart rebuilds `Vec<ChartLine>` from scratch every frame — 5× filter + normalize + clone | `app.rs:1314` | Cache `Vec<ChartLine>`; rebuild only on overlay/window/data change | [ ] |
| PERF-C5 | Critical | Price picker re-collects filtered candidate list every frame while open | `app.rs:1998` | Cache candidates; recompute only when `price_picker_filter` changes | [ ] |
| PERF-H1 | High | Screen-point mapping creates new `Vec<Pos2>` every frame — 250 pts for VIX, 5×250 for correlation | `app.rs:1704` | Cache screen points; remap only when chart rect, zoom, or data changes | [ ] |
| PERF-H2 | High | Activity log re-formats timestamps and counts with `format!()` every render frame | `app.rs:555, 584` | Store pre-formatted strings in `LogEntry` at creation time | [ ] |
| PERF-H3 | High | Y-axis label strings allocated every frame — ~15 `format!()` calls/frame across 3 charts | `app.rs:1622` | Cache label strings; invalidate when y bounds change | [ ] |
| PERF-M1 | Medium | `activity_log` grows without bound within a session | `app.rs:149` | Cap at max entries (e.g. 500); drop oldest when exceeded | [ ] |
| PERF-M2 | Medium | `Observation.source` is a duplicate heap `String` per row — `"FRED VIXCLS"` allocated ~10k times | `models.rs:122` | Replace with enum or `&'static str` discriminant | [ ] |
| PERF-M3 | Medium | No index on `observations(instrument)` — full table scan on every load | `storage.rs` | `CREATE INDEX IF NOT EXISTS idx_obs_instrument ON observations(instrument, date)` | [ ] |
| PERF-M4 | Medium | Spike episode rolling window re-sorts full window from scratch each step | `analysis.rs:95` | Use incremental sliding-window percentile or pre-sort + binary search | [ ] |
| PERF-L1 | Low | Unused `_right_color` parameter in `collapsible_chart_header` — all callers pass `TEXT_PRIMARY` | `app.rs:1127` | Remove the parameter and update call sites | [ ] |
| PERF-L2 | Low | `ChartLine.label` is `String` — heap allocation for static strings like `"VIX"` | `app.rs:1262` | Change to `&'static str` or `Cow<'static, str>` | [ ] |
| PERF-L3 | Low | Cache invalidation one frame late after threshold slider drag | `app.rs:211` | Call `refresh_analysis_cache()` immediately after mutating `threshold_config` | [ ] |

---

## Summary

| Area | Critical | High | Medium | Low | Done | Won't Fix | Total |
|------|----------|------|--------|-----|------|-----------|-------|
| Security | 0 | 4 | 5 | 2 | 2 | 4 | **15** |
| Performance | 5 | 3 | 4 | 3 | 0 | 0 | **15** |
| **Total** | **5** | **7** | **9** | **5** | **2** | **4** | **30** |
